### Logging in figure1

Configuration for logging is handled through the configuration package. The default level 
for most of the logs is set to info, though the defaults for library packages are set to
error. 
This is described in more detail in figure1.configuration.settings which also shows the default
settings.
